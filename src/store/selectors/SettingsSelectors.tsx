// import {useSelector} from "react-redux";
// import {RootState} from "../store";
export const onboarding = ({state}) => state.settings.onboarding;
export const cities = ({state}) => state.settings.cities;

// export const onboarding = useSelector((state: RootState) => state.settings.onboarding);
